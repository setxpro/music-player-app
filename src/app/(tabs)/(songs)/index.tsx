import { defaultStyles } from '@/styles'
import { View } from 'react-native'

const SongScreen = () => {
	return (
		<View style={defaultStyles.container}>
			{/* <Text style={defaultStyles.text}>Songs Screean</Text> */}
		</View>
	)
}

export default SongScreen
