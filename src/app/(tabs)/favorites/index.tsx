import { defaultStyles } from '@/styles'
import { View } from 'react-native'

const FavoritesScreen = () => {
	return (
		<View style={defaultStyles.container}>
			{/* <Text style={defaultStyles.text}>Favoritess Screean</Text> */}
		</View>
	)
}

export default FavoritesScreen
